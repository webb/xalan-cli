apply plugin: 'java'

repositories {
    mavenCentral()
}

dependencies {
    // https://mvnrepository.com/artifact/xalan/xalan
    runtime group: 'xalan', name: 'xalan', version: '2.7.2'
}

task save_jars(type:Copy) {
     from configurations.runtime
     into "${project.jars_dir}"
}
